rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any user to read and write all documents. 
    // This removes the need for Firebase Auth completely.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
